import type { NavItemConfig } from '@/types/nav';

export const navItems = [
    {
        key: 'tasks',
        title: 'Tasks',
        href: '/tasks',
        icon: 'task',
    },
] satisfies NavItemConfig[];
